#pragma once
#include "config.h"
namespace PowershellTweaks
{
	int DeletePackages(config::apps apps);
	int ActivateWindows();
	int EnableUltimatePerf(config cfg);
	int DisableDynamicTick(config cfg);
};

